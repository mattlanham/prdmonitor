## Codebase Patterns

### Project Structure
- Entry point: `main.go` with minimal logic, delegates to internal packages
- Data models in `internal/model/model.go`
- Tests use Go's `testing` package with `t.TempDir()` for temp directories
- Config struct pattern for CLI configuration

### CLI Argument Parsing
- `ParseArgs(args []string)` takes os.Args to allow testing
- Returns `*Config, error` pattern for validation
- Errors are printed to stderr, exit code 1 for failures

### Error Handling
- Use `fmt.Errorf` with `%w` for wrapping errors
- Check `os.IsNotExist(err)` for cleaner error messages
- Validate directory is readable by attempting to open it

### Scanner Package
- Use `filepath.WalkDir` for recursive directory traversal (more efficient than `filepath.Walk`)
- Return `*ScanResult` with both files and non-fatal errors
- Convert paths to absolute using `filepath.Abs()` for consistency
- Custom error types (e.g., `NotADirectoryError`) for specific error handling
- Module name is `lanham/prdmonitor` (important for internal imports)

### Parser Package
- `ParseFile(filePath)` returns `*ParseResult, error` - reads and parses a single file
- `ParseBytes(data)` returns `*model.PRD, error` - parses JSON bytes directly (useful for testing)
- `ParseFiles([]string)` returns `([]*ParseResult, []error)` - batch parsing with non-fatal error collection
- Custom `ParseError` type wraps file path with underlying error, implements `Unwrap()` for error chains
- Use `errors.As()` to check for specific error types in tests
- JSON unmarshaling handles missing fields gracefully (zero values)

### TUI Package (Bubble Tea)
- Uses Model-Update-View pattern with `tea.Model` interface
- `App` is the main model, contains `Board` which contains `Column` components
- `tui.Run(parseResults)` is the main entry point - launches the TUI program
- `tui.RunWithWatcher(parseResults, watcher, rootDir)` for file watching support
- `tea.WithAltScreen()` provides clean terminal switching
- `tea.WithMouseCellMotion()` enables mouse event handling (wheel scroll)
- `tea.WindowSizeMsg` is used to handle terminal resize events
- `tea.MouseMsg` handles mouse events with Type: `tea.MouseWheelUp`, `tea.MouseWheelDown`
- `tea.Cmd` pattern for async operations (e.g., listening for file change events)
- Custom message types (e.g., `FileChangedMsg`) for external events
- Lip Gloss styles: use `lipgloss.NewStyle()` for creating styles, chain methods
- `lipgloss.JoinHorizontal()` and `lipgloss.JoinVertical()` for layout composition
- Card component with box-drawing borders using `lipgloss.RoundedBorder()`
- `truncateString()` helper function for text that may exceed display width
- Column colors: Red (203) for Incomplete, Yellow (220) for In Progress, Green (84) for Complete

### Scrollable Column Pattern
- Each Column tracks its own `scrollOffset` for independent scrolling
- `visibleCardCount()` estimates visible cards based on column height (~6 lines per card)
- `maxScrollOffset()` returns `len(cards) - visibleCount` (clamped to 0)
- `ensureCardVisible(cardIndex)` auto-adjusts scroll offset to keep card in view
- Navigation methods (MoveUp/MoveDown) call `ensureSelectedCardVisible()` after updating selection
- View() renders only cards in range `[scrollOffset, scrollOffset+visibleCount]`
- Scroll indicators: `▲ more above` / `▼ more below` for visual feedback
- Position indicator: `(1-5 of 20)` shows current visible range
- Mouse wheel: `tea.MouseMsg.X / columnWidth` determines target column

### Watcher Package (fsnotify)
- `watcher.New()` creates a new file watcher
- `watcher.AddFile(path)` / `watcher.AddFiles(paths)` to watch files
- `watcher.WatchDirectory(rootDir)` recursively watches directory tree for new prd.json files
- Watches parent directories since fsnotify works better with directories
- Filters events to only watched files (ignores other files in same directory)
- `watcher.Events()` returns channel for receiving `Event` structs
- `watcher.Start()` should be called in a goroutine
- `watcher.Stop()` closes the watcher and releases resources
- Debouncing: 100ms between events for the same file to handle rapid saves
- Operation types: `OpModify`, `OpCreate`, `OpDelete`
- Use `defer w.Stop()` in main.go for proper cleanup
- **Path Resolution**: Use `filepath.EvalSymlinks()` to resolve symlinks for consistent path comparison
  - Critical on macOS where `/var` is symlink to `/private/var`
  - fsnotify reports resolved paths, so stored paths must also be resolved
- **New File Detection**: `WatchDirectory()` enables detecting new prd.json files
  - Watches all subdirectories recursively
  - Detects new directories and adds them to watch list
  - Only emits events for files named exactly "prd.json"
- **Debounce Ordering**: Check operation type BEFORE debouncing
  - Prevents irrelevant events (like CHMOD) from affecting debounce timing for WRITE events

### Card Component Pattern
- Separate `card.go` file for the Card component per AGENTS.md project structure
- `Card` struct holds `ProjectName` and `Story` (model.UserStory)
- `NewCard(projectName, story)` constructor for creating cards
- `Card.Render(width)` method returns styled string output
- `CardStyle` struct for customizable styling (border, project, ID, title, description colors)
- `DefaultCardStyle()` provides sensible defaults
- `RenderWithStyle(width, style)` for custom styling
- Card layout: Project name at top, Story ID (prominent), Title (bold), Description (truncated)

### Read-Only Interface Pattern
- Use constants for status messages (e.g., `ReadOnlyMessage`, `EditHelpMessage`) for testability
- Status bar at bottom shows mode indicator and shortcuts
- Include helpful message guiding users to edit source files directly
- Explicitly handle and ignore edit-related keybindings (e, i, d, x, enter, backspace, delete)
- Return `nil` cmd from Update() to indicate "no action" for ignored keys
- `tea.KeyMsg` has specific types: `tea.KeyEnter`, `tea.KeyBackspace`, `tea.KeyDelete` vs. `tea.KeyRunes`

### Keyboard Navigation Pattern
- Navigation state tracked in Board: `selectedCol` (0-2) and `selectedCard` (index within column)
- `MoveLeft/Right/Up/Down()` methods on Board handle navigation with clamping
- `CycleColumn()` wraps around for Tab key behavior
- `SelectedCard()` returns currently selected Card or nil for empty columns
- Selection state flows: App → Board.View() sets Column selection → Column.View() highlights Card
- Column stores `isSelected` and `selectedCard` for rendering
- Card has `RenderSelected(width, isSelected)` for selection highlighting

### Overlay Pattern (Help, Expanded Card)
- App tracks overlay state: `showHelp bool`, `expandedCard *Card`
- Overlays block navigation - check overlay state before processing navigation keys
- Esc key closes overlays in priority order (expanded card first, then help)
- ? key toggles help overlay (closes expanded card first if open)
- `renderWithOverlay()` centers overlay content over base view
- HelpOverlay component with `HelpShortcuts` array for documentation

### Card Expansion Pattern
- `RenderExpanded(width)` shows full card details: title, description, acceptance criteria, priority, status
- Use `wrapText()` helper to wrap long text within width
- Status colors: Green (84) for complete, Yellow (220) for in-progress, Red (203) for incomplete
- Footer hint shows how to close expanded view

---

## 2026-01-18 - PM-001
- **What was implemented:** CLI argument parsing for folder selection with validation
- **Files changed:**
  - `main.go` - Entry point with ParseArgs and ValidateFolder functions
  - `main_test.go` - 7 test cases for argument parsing and folder validation
  - `internal/model/model.go` - Data structures for PRD and UserStory
- **Tests added:**
  - TestParseArgs_WithValidFolder
  - TestParseArgs_DefaultToCurrentDirectory
  - TestParseArgs_NonExistentFolder
  - TestParseArgs_FileNotDirectory
  - TestValidateFolder_ValidDirectory
  - TestValidateFolder_NonExistent
  - TestValidateFolder_NotADirectory
- **Learnings:**
  - Use `t.TempDir()` for creating temp directories in tests (auto-cleaned up)
  - Pass `[]string` for args to make functions testable without mocking os.Args
  - Check directory readability by attempting `os.Open()` on it
  - Go 1.24 is the current version (updated from AGENTS.md which says 1.21+)
---

## 2026-01-18 - PM-002
- **What was implemented:** Recursive directory scanning for prd.json files
- **Files changed:**
  - `internal/scanner/scanner.go` - New package with Scan function and ScanResult type
  - `internal/scanner/scanner_test.go` - Comprehensive test suite (10 test cases)
  - `main.go` - Integrated scanner, displays project count on startup
- **Tests added:**
  - TestScan_FindsPrdJsonFiles - Tests recursive discovery in nested directories
  - TestScan_IgnoresOtherFiles - Verifies only exact "prd.json" matches
  - TestScan_EmptyDirectory - Tests empty directory handling
  - TestScan_NonExistentDirectory - Tests error for invalid paths
  - TestScan_FileInsteadOfDirectory - Tests NotADirectoryError
  - TestScan_ReturnsAbsolutePaths - Verifies paths are absolute
  - TestScan_HandlesUnreadableDirectories - Tests graceful error handling
  - TestScanResult_Count - Tests Count() method
  - TestScanResult_HasErrors - Tests HasErrors() method
  - TestNotADirectoryError - Tests error message format
- **Learnings:**
  - `filepath.WalkDir` is preferred over `filepath.Walk` (more efficient, added in Go 1.16)
  - Use `d.Name()` instead of `filepath.Base(path)` when inside WalkDir callback
  - Return struct with both results and errors for non-fatal error handling
  - Custom error types allow callers to check for specific error conditions
  - Module name `lanham/prdmonitor` must be used in all internal imports
---

## 2026-01-18 - PM-003
- **What was implemented:** JSON parsing and validation for prd.json files
- **Files changed:**
  - `internal/parser/parser.go` - New package with ParseFile, ParseBytes, and ParseFiles functions
  - `internal/parser/parser_test.go` - Comprehensive test suite (18 test cases)
  - `main.go` - Integrated parser to parse all discovered files and display user story counts
- **Tests added:**
  - TestParseBytes_ValidJSON - Tests parsing of complete valid JSON
  - TestParseBytes_ExtractsProjectName - Tests project name extraction
  - TestParseBytes_ExtractsUserStoryFields - Tests all user story fields
  - TestParseBytes_MultipleUserStories - Tests parsing multiple stories
  - TestParseBytes_EmptyUserStories - Tests empty story array
  - TestParseBytes_MalformedJSON - Tests various malformed JSON inputs
  - TestParseBytes_MissingFields - Tests graceful handling of missing fields
  - TestParseFile_ValidFile - Tests file reading and parsing
  - TestParseFile_NonExistentFile - Tests error for missing files
  - TestParseFile_MalformedFile - Tests error for invalid JSON in files
  - TestParseError_Error - Tests error message formatting
  - TestParseError_Unwrap - Tests error unwrapping
  - TestParseFiles_MultipleFiles - Tests batch parsing
  - TestParseFiles_WithErrors - Tests mixed valid/invalid file parsing
  - TestParseFiles_NonExistentFiles - Tests all files failing
  - TestParseFiles_EmptyList - Tests empty input
  - TestParseBytes_AllStatusValues - Tests all status value parsing
- **Learnings:**
  - Use `os.ReadFile()` for simple file reading (replaces ioutil.ReadFile in newer Go)
  - Custom error types should implement `Unwrap()` for error chain inspection
  - Use `errors.As()` to check for specific error types (not type assertion)
  - `encoding/json` gracefully handles missing JSON fields with zero values
  - Batch processing pattern: return `(results, errors)` tuple for non-fatal error handling
  - Test table-driven tests with `t.Run()` for clearer test organization
---

## 2026-01-18 - PM-004
- **What was implemented:** Terminal UI with Kanban board layout using Bubble Tea
- **Files changed:**
  - `internal/tui/app.go` - Main Bubble Tea model with Model-Update-View pattern
  - `internal/tui/board.go` - Kanban board with three columns, distributes cards by status
  - `internal/tui/column.go` - Column component with styling, card rendering, and truncation
  - `internal/tui/tui_test.go` - Comprehensive test suite (13 test cases)
  - `main.go` - Integrated TUI launch after parsing
  - `go.mod`, `go.sum` - Added Bubble Tea and Lip Gloss dependencies
- **Tests added:**
  - TestNewColumn - Tests column creation
  - TestColumn_AddCard - Tests adding cards to columns
  - TestNewBoard_DistributesCardsByStatus - Tests status-based card distribution
  - TestNewBoard_UnknownStatusDefaultsToIncomplete - Tests unknown status handling
  - TestNewBoard_MultipleProjects - Tests aggregation from multiple PRDs
  - TestNewBoard_EmptyParseResults - Tests empty input handling
  - TestColumn_View_EmptyColumn - Tests empty column placeholder message
  - TestColumn_View_WithCards - Tests column rendering with cards
  - TestTruncate - Tests string truncation helper
  - TestNewApp - Tests app initialization
  - TestApp_Init - Tests Bubble Tea init
  - TestBoard_SetSize - Tests responsive sizing
- **Learnings:**
  - Bubble Tea uses `tea.Model` interface with Init(), Update(), View() methods
  - `tea.WithAltScreen()` provides clean terminal enter/exit
  - `tea.WindowSizeMsg` is sent when terminal resizes - use for responsive layout
  - `tea.KeyMsg` for keyboard input, check `msg.String()` for key name
  - Lip Gloss method chaining pattern: `lipgloss.NewStyle().Bold(true).Foreground(...)`
  - Use `lipgloss.Color("39")` for ANSI color codes (strings, not ints)
  - `lipgloss.JoinHorizontal(lipgloss.Top, ...)` for side-by-side layout
  - `lipgloss.RoundedBorder()` for nice box-drawing borders
  - Column width calculation: `(terminalWidth - margins) / numColumns`
  - TUI testing focuses on data structures since View() output is hard to assert
---

## 2026-01-18 - PM-005
- **What was implemented:** User story card display component with styled layout
- **Files changed:**
  - `internal/tui/card.go` - New dedicated Card component with Render methods
  - `internal/tui/board.go` - Removed duplicate Card struct, uses NewCard constructor
  - `internal/tui/column.go` - Uses Card.Render() instead of local renderCard function
  - `internal/tui/tui_test.go` - Added 12 new tests for Card component
- **Tests added:**
  - TestNewCard - Tests card constructor
  - TestCard_Render_ContainsProjectName - Verifies project name display
  - TestCard_Render_ContainsStoryID - Verifies ID is prominently displayed
  - TestCard_Render_ContainsTitle - Verifies title is displayed
  - TestCard_Render_ContainsDescription - Verifies description is displayed
  - TestCard_Render_TruncatesLongDescription - Tests description truncation
  - TestCard_Render_EmptyDescription - Tests handling of empty description
  - TestCard_Render_HasBorder - Verifies box-drawing characters are used
  - TestCard_RenderWithStyle - Tests custom styling
  - TestDefaultCardStyle - Verifies default style values
  - TestCard_Render_MinimumWidth - Tests small width handling
  - TestTruncateString - Tests string truncation helper (renamed from TestTruncate)
- **Learnings:**
  - Separate component files (card.go) follow AGENTS.md project structure
  - Use constructor functions (NewCard) for cleaner API and easier testing
  - CardStyle struct pattern allows customizable styling without changing component logic
  - Render methods should handle edge cases (empty strings, minimum widths)
  - When refactoring, check for duplicate struct definitions across files
  - Use `strings.ContainsAny()` to check for any of multiple characters (e.g., border chars)
  - lipgloss.Color type is just a string alias, works with string color codes
---

## 2026-01-18 - PM-006
- **What was implemented:** Status-based card sorting with priority ordering within columns
- **Files changed:**
  - `internal/tui/column.go` - Added SortByPriority() method using sort.Slice
  - `internal/tui/board.go` - Added sorting calls after distributing cards to columns
  - `internal/tui/tui_test.go` - Added 6 new tests for sorting functionality
- **Tests added:**
  - TestColumn_SortByPriority - Tests cards are sorted by priority (lower values first)
  - TestColumn_SortByPriority_EmptyColumn - Tests empty column handling
  - TestColumn_SortByPriority_SingleCard - Tests single card edge case
  - TestColumn_SortByPriority_SamePriority - Tests cards with equal priority
  - TestNewBoard_SortsCardsByPriority - Tests board sorts cards during construction
  - TestNewBoard_SortsAcrossMultipleProjects - Tests sorting works across projects
- **Learnings:**
  - Status-based distribution was already implemented in PM-004, PM-006 focused on sorting within columns
  - Go's `sort.Slice` is the idiomatic way to sort slices with custom comparators
  - `sort.Slice` is not stable (equal elements may be reordered), use `sort.SliceStable` if order preservation is needed
  - Card struct contains full `model.UserStory` so all fields (including Priority) are accessible without modifications
  - When acceptance criteria seems already implemented, look deeper for additional functionality like sorting
---

## 2026-01-18 - PM-007
- **What was implemented:** File change detection and live updates using fsnotify
- **Files changed:**
  - `internal/watcher/watcher.go` - New package with file watching using fsnotify
  - `internal/watcher/watcher_test.go` - 12 test cases for watcher functionality
  - `internal/tui/app.go` - Added FileChangedMsg, listenForFileChanges(), handleFileChange()
  - `internal/tui/tui_test.go` - Updated NewApp tests for new signature
  - `main.go` - Integrated watcher with TUI, graceful degradation if watcher fails
  - `go.mod`, `go.sum` - Added fsnotify dependency
- **Tests added:**
  - TestNew - Tests watcher creation
  - TestAddFile - Tests adding a file to watch
  - TestAddFile_Duplicate - Tests idempotent file adding
  - TestAddFiles - Tests batch file adding
  - TestRemoveFile - Tests removing a file from watching
  - TestWatchedFiles - Tests listing watched files
  - TestOperation_String - Tests operation string representation
  - TestWatcher_DetectsFileModification - Integration test for file modification detection
  - TestWatcher_IgnoresUnwatchedFiles - Tests filtering to only watched files
  - TestWatcher_Stop - Tests clean shutdown
  - TestWatcher_Events - Tests event channel access
  - TestWatcher_Errors - Tests error channel access
- **Learnings:**
  - fsnotify works better watching directories than individual files
  - Watch the parent directory and filter events to specific files we care about
  - Debouncing is important - editors often save files in multiple write operations
  - Use `select` with channels to handle multiple event sources (events, errors, done)
  - Bubble Tea async pattern: Init() returns a tea.Cmd that blocks on channel, returns msg when event occurs
  - After handling a FileChangedMsg, must return another listenForFileChanges() cmd to continue listening
  - When updating function signatures, check all call sites including test files
  - Graceful degradation: if watcher fails to start, continue without file watching
---

## 2026-01-18 - PM-008
- **What was implemented:** New file detection for prd.json files in existing and new subdirectories
- **Files changed:**
  - `internal/watcher/watcher.go` - Added `resolvePath()` helper, `WatchDirectory()`, `handleNewFileOrDirectory()`, `watchNewDirectory()`. Fixed debounce ordering bug.
  - `internal/watcher/watcher_test.go` - Added 5 new tests for new file detection, updated existing tests to use resolved paths
  - `ralph/progress.txt` - Updated Watcher Package patterns with new file detection documentation
- **Tests added:**
  - TestResolvePath - Tests symlink resolution for consistent path handling
  - TestWatchDirectory - Tests recursive directory watching
  - TestWatcher_DetectsNewPrdJsonFile - Tests new prd.json file detection in root directory
  - TestWatcher_DetectsNewSubdirectoryWithPrdJson - Tests detection in newly created subdirectories
  - TestWatcher_IgnoresNonPrdJsonFiles - Tests that only prd.json files trigger events
  - TestWatcher_DetectsNewPrdJsonInExistingSubdirectory - Tests detection in pre-existing subdirectories
- **Learnings:**
  - macOS `/var` is a symlink to `/private/var` - must use `filepath.EvalSymlinks()` for path resolution
  - fsnotify reports the resolved (real) path, not the symlink path used when adding the watch
  - Tests must use resolved paths when comparing event file paths
  - `resolvePath()` helper should handle non-existent files by resolving the parent directory
  - Debounce logic must check operation type BEFORE updating debounce timestamp
    - CHMOD events from `os.WriteFile()` were incorrectly affecting WRITE event timing
    - Move operation type switch BEFORE debounce check to skip irrelevant events early
  - Empty fsnotify events (Name == "") occur when watcher is closing - filter them out
  - Directory watching with `WatchDirectory()` sets `watchNewDirs` flag for new file detection mode
---

## 2026-01-18 - PM-009
- **What was implemented:** Read-only board interface with status bar and help messages
- **Files changed:**
  - `internal/tui/app.go` - Added ReadOnlyMessage and EditHelpMessage constants, enhanced footer with status bar and help message, explicitly ignore edit keybindings
  - `internal/tui/tui_test.go` - Added 6 new tests for read-only functionality
- **Tests added:**
  - TestReadOnlyMessage_Constant - Verifies read-only message constant exists
  - TestEditHelpMessage_Constant - Verifies edit help message mentions prd.json
  - TestApp_View_ContainsReadOnlyIndicator - Tests status bar displays VIEW-ONLY
  - TestApp_View_ContainsEditHelpMessage - Tests footer shows prd.json editing guidance
  - TestApp_Update_IgnoresEditKeyBindings - Tests e, i, d, x, enter, backspace, delete keys are ignored
  - TestApp_Update_QuitStillWorks - Tests q key still quits in read-only mode
- **Learnings:**
  - Read-only enforcement is twofold: (1) don't implement edit features, (2) explicitly ignore edit keybindings
  - Use constants for status messages to make them testable and consistent
  - `tea.KeyMsg` has specific types for special keys (KeyEnter, KeyBackspace, KeyDelete) vs. KeyRunes for letters
  - The footer/status bar should have both the mode indicator AND helpful guidance for users
  - Returning `nil` cmd from Update() is proper way to indicate "no action" for ignored keys
---

## 2026-01-18 - PM-010
- **What was implemented:** File deletion handling - detects when prd.json files are deleted, removes them from tracking, and removes associated cards from the board
- **Files changed:**
  - `internal/watcher/watcher.go` - Added cleanup logic to remove deleted files from tracking map after OpDelete events
  - `internal/watcher/watcher_test.go` - Added 3 new tests for file deletion detection and cleanup
  - `internal/tui/tui_test.go` - Added 4 new tests for TUI handling of file deletion events
- **Tests added:**
  - TestWatcher_DetectsFileDeletion - Tests that deleted files emit OpDelete events
  - TestWatcher_RemovesDeletedFileFromTracking - Tests that deleted files are removed from watcher's file map
  - TestWatcher_DetectsFileRenameAsDelete - Tests that file renames are treated as deletions
  - TestApp_HandleFileChange_Delete - Tests removal of project cards when file is deleted
  - TestApp_HandleFileChange_DeleteLastProject - Tests deletion of the only project leaves empty board
  - TestApp_HandleFileChange_DeleteNonExistent - Tests deleting non-existent file doesn't affect board
  - TestApp_HandleFileChange_DeleteMultipleStories - Tests all cards removed, no orphans remain
- **Learnings:**
  - fsnotify handles both Remove and Rename as deletion events for watched files
  - When a file is deleted, must clean up the watcher's internal tracking map to prevent stale references
  - File deletion handling was mostly already implemented in PM-007/PM-008 - this story focused on cleanup and comprehensive testing
  - Test cases for deletion should cover: single project removal, last project removal, non-existent file deletion, and multi-story projects
---

## 2026-01-18 - PM-011
- **What was implemented:** Multi-project aggregation - verified and tested existing functionality with comprehensive test coverage for 20+ projects
- **Files changed:**
  - `internal/tui/tui_test.go` - Added 7 new tests for multi-project aggregation (20+ project performance, project name distinction, cross-project sorting)
- **Tests added:**
  - TestNewBoard_Handles20PlusProjects - Tests board with 25 projects (75 total cards) without performance issues
  - TestNewBoard_MultiProjectAggregation_CardsDistinguishedByProjectName - Tests distinct project names on cards
  - TestNewBoard_MultiProject_SortsByPriorityAcrossAllProjects - Tests priority sorting across different projects
  - TestBoard_View_With20PlusProjects - Tests View() method with 20 projects
  - TestApp_Update_With20PlusProjects - Tests App Update/Init with 20 projects
  - TestMultiProject_CardsFromAllStatusesAggregated - Tests cards from multiple projects across all status columns
- **Learnings:**
  - Multi-project aggregation was already fully implemented in PM-004 (NewBoard iterates all parseResults)
  - PM-011 focused on validation and comprehensive testing of the 20+ project requirement
  - Existing implementation handles project name display (Card.ProjectName), status-based sorting (SortByPriority), and cross-project aggregation correctly
  - Test coverage for scale (20+ projects) is important for performance guarantees even when functionality already exists
  - When acceptance criteria seems already implemented, focus on adding comprehensive tests to verify all requirements
---

## 2026-01-18 - PM-012
- **What was implemented:** Full keyboard navigation with arrow keys, hjkl vim-style keys, Tab column cycling, Enter/Space card expansion, ? help overlay, and Esc to close overlays
- **Files changed:**
  - `internal/tui/app.go` - Added showHelp, expandedCard, helpOverlay fields to App; enhanced Update() with navigation keybindings; updated View() to render overlays
  - `internal/tui/board.go` - Added selectedCol, selectedCard fields; implemented MoveLeft/Right/Up/Down, CycleColumn, clampSelectedCard, SelectedCard methods
  - `internal/tui/column.go` - Added isSelected, selectedCard fields; implemented SetSelected() for selection state
  - `internal/tui/card.go` - Added RenderSelected(), SelectedCardStyle(), RenderExpanded(), wrapText(), itoa(), min() helper functions
  - `internal/tui/help.go` - New file: HelpOverlay component with HelpShortcuts array and View() method
  - `internal/tui/tui_test.go` - Added 35 new tests for keyboard navigation, help overlay, card expansion
- **Tests added:**
  - TestBoard_MoveRight/Left/UpDown - Tests directional navigation
  - TestBoard_CycleColumn - Tests Tab wrapping through columns
  - TestBoard_SelectedCard - Tests card selection and retrieval
  - TestBoard_SelectedCard_EmptyColumn - Tests empty column handling
  - TestBoard_NavigationClampsCardIndex - Tests index clamping when changing columns
  - TestApp_Update_ArrowKeyNavigation - Tests arrow key handling
  - TestApp_Update_HjklNavigation - Tests vim-style navigation
  - TestApp_Update_TabCyclesColumns - Tests Tab key behavior
  - TestApp_Update_EnterExpandsCard - Tests Enter key for card expansion
  - TestApp_Update_SpaceExpandsCard - Tests Space key for card expansion
  - TestApp_Update_EscClosesExpandedCard - Tests Esc closes expanded card
  - TestApp_Update_QuestionMarkTogglesHelp - Tests ? toggles help overlay
  - TestApp_Update_EscClosesHelp - Tests Esc closes help overlay
  - TestApp_Update_HelpBlocksNavigation - Tests navigation blocked when help showing
  - TestApp_View_ShowsHelpOverlay - Tests help overlay rendering
  - TestApp_View_ShowsExpandedCard - Tests expanded card rendering
  - TestNewHelpOverlay - Tests help overlay creation
  - TestHelpOverlay_SetSize/View - Tests help overlay sizing and rendering
  - TestHelpShortcuts_Contains_AllRequiredKeys - Verifies all shortcuts documented
  - TestCard_RenderExpanded - Tests full card details view
  - TestCard_RenderExpanded_EmptyAcceptanceCriteria - Tests edge case
  - TestCard_RenderSelected - Tests selected card styling
  - TestSelectedCardStyle - Tests selected style colors
  - TestColumn_SetSelected - Tests column selection state
  - TestWrapText/Itoa/Min - Tests helper functions
- **Learnings:**
  - Bubble Tea's `tea.KeyMsg` has both `Type` (for special keys like Enter, Esc, arrows) and `String()` (for printable characters)
  - Arrow keys use `tea.KeyUp/Down/Left/Right` types, while letters use `tea.KeyRunes`
  - Selection state should flow: App → Board → Column → Card for proper rendering
  - Overlays (help, expanded card) should block navigation until closed
  - Use `tea.KeyEsc` for closing overlays - it's the standard escape pattern
  - Help overlay pattern: store shortcuts in a struct array for easy rendering and testing
  - Card expansion shows full description, acceptance criteria, priority, and status
  - Selection highlighting uses different border color (cyan) to distinguish from default (gray)
  - Navigation clamping is important when columns have different card counts
---

## 2026-01-18 - PM-013
- **What was implemented:** Scrollable columns with scroll position tracking, visual indicators, keyboard-triggered scrolling, and mouse wheel support
- **Files changed:**
  - `internal/tui/column.go` - Added scrollOffset field, ScrollUp/ScrollDown methods, ensureCardVisible(), visibleCardCount(), scroll position indicator rendering
  - `internal/tui/board.go` - Added ScrollColumnUp/ScrollColumnDown methods and ensureSelectedCardVisible() for keyboard navigation
  - `internal/tui/app.go` - Added handleMouseEvent() for mouse wheel scrolling, enabled tea.WithMouseCellMotion()
  - `internal/tui/tui_test.go` - Added 21 new tests for scrolling functionality
- **Tests added:**
  - TestColumn_ScrollOffset_Initial - Tests initial scroll offset is 0
  - TestColumn_VisibleCardCount - Tests visible card calculation
  - TestColumn_ScrollUp - Tests scroll up with clamping at top
  - TestColumn_ScrollDown - Tests scroll down with clamping at max
  - TestColumn_SetScrollOffset - Tests direct scroll offset setting with clamping
  - TestColumn_MaxScrollOffset - Tests max offset calculation
  - TestColumn_EnsureCardVisible_ScrollsDown - Tests auto-scroll when navigating down
  - TestColumn_EnsureCardVisible_ScrollsUp - Tests auto-scroll when navigating up
  - TestColumn_View_ShowsScrollIndicator_WhenScrollable - Tests "(X-Y of Z)" position indicator
  - TestColumn_View_ShowsMoreBelowIndicator - Tests "more below" arrow indicator
  - TestColumn_View_HidesScrollIndicators_WhenNoScrollNeeded - Tests indicators hidden when all cards fit
  - TestBoard_ScrollColumnUp - Tests board-level scroll up
  - TestBoard_ScrollColumnDown - Tests board-level scroll down
  - TestColumn_IndependentScrolling - Tests each column scrolls independently
  - TestApp_KeyboardNavigation_TriggersScrolling - Tests MoveUp/MoveDown triggers scroll adjustment
  - TestApp_MouseWheelScroll - Tests mouse wheel up/down events
  - TestApp_MouseWheelScroll_BlockedByOverlay - Tests mouse scroll blocked when overlay shown
- **Learnings:**
  - Each Column stores its own `scrollOffset` for independent scrolling
  - `visibleCardCount()` estimates how many cards fit based on column height and card height (~6 lines per card)
  - `ensureCardVisible()` is called during navigation to keep selected card in view
  - Navigation methods (MoveUp/MoveDown) must trigger scroll adjustment, not just View()
  - Mouse events use `tea.MouseMsg` with Type: `tea.MouseWheelUp`, `tea.MouseWheelDown`
  - Enable mouse support with `tea.WithMouseCellMotion()` in NewProgram options
  - Column position for mouse events is determined by `msg.X / columnWidth`
  - Scroll indicators (▲ ▼) provide visual feedback about content above/below
  - Position indicator format "(1-5 of 20)" shows current visible range
---
