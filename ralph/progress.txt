## Codebase Patterns

### Project Structure
- Entry point: `main.go` with minimal logic, delegates to internal packages
- Data models in `internal/model/model.go`
- Tests use Go's `testing` package with `t.TempDir()` for temp directories
- Config struct pattern for CLI configuration

### CLI Argument Parsing
- `ParseArgs(args []string)` takes os.Args to allow testing
- Returns `*Config, error` pattern for validation
- Errors are printed to stderr, exit code 1 for failures

### Error Handling
- Use `fmt.Errorf` with `%w` for wrapping errors
- Check `os.IsNotExist(err)` for cleaner error messages
- Validate directory is readable by attempting to open it

### Scanner Package
- Use `filepath.WalkDir` for recursive directory traversal (more efficient than `filepath.Walk`)
- Return `*ScanResult` with both files and non-fatal errors
- Convert paths to absolute using `filepath.Abs()` for consistency
- Custom error types (e.g., `NotADirectoryError`) for specific error handling
- Module name is `lanham/prdmonitor` (important for internal imports)

### Parser Package
- `ParseFile(filePath)` returns `*ParseResult, error` - reads and parses a single file
- `ParseBytes(data)` returns `*model.PRD, error` - parses JSON bytes directly (useful for testing)
- `ParseFiles([]string)` returns `([]*ParseResult, []error)` - batch parsing with non-fatal error collection
- Custom `ParseError` type wraps file path with underlying error, implements `Unwrap()` for error chains
- Use `errors.As()` to check for specific error types in tests
- JSON unmarshaling handles missing fields gracefully (zero values)

### TUI Package (Bubble Tea)
- Uses Model-Update-View pattern with `tea.Model` interface
- `App` is the main model, contains `Board` which contains `Column` components
- `tui.Run(parseResults)` is the main entry point - launches the TUI program
- `tea.WithAltScreen()` provides clean terminal switching
- `tea.WindowSizeMsg` is used to handle terminal resize events
- Lip Gloss styles: use `lipgloss.NewStyle()` for creating styles, chain methods
- `lipgloss.JoinHorizontal()` and `lipgloss.JoinVertical()` for layout composition
- Card component with box-drawing borders using `lipgloss.RoundedBorder()`
- `truncate()` helper function for text that may exceed display width
- Column colors: Red (203) for Incomplete, Yellow (220) for In Progress, Green (84) for Complete

---

## 2026-01-18 - PM-001
- **What was implemented:** CLI argument parsing for folder selection with validation
- **Files changed:**
  - `main.go` - Entry point with ParseArgs and ValidateFolder functions
  - `main_test.go` - 7 test cases for argument parsing and folder validation
  - `internal/model/model.go` - Data structures for PRD and UserStory
- **Tests added:**
  - TestParseArgs_WithValidFolder
  - TestParseArgs_DefaultToCurrentDirectory
  - TestParseArgs_NonExistentFolder
  - TestParseArgs_FileNotDirectory
  - TestValidateFolder_ValidDirectory
  - TestValidateFolder_NonExistent
  - TestValidateFolder_NotADirectory
- **Learnings:**
  - Use `t.TempDir()` for creating temp directories in tests (auto-cleaned up)
  - Pass `[]string` for args to make functions testable without mocking os.Args
  - Check directory readability by attempting `os.Open()` on it
  - Go 1.24 is the current version (updated from AGENTS.md which says 1.21+)
---

## 2026-01-18 - PM-002
- **What was implemented:** Recursive directory scanning for prd.json files
- **Files changed:**
  - `internal/scanner/scanner.go` - New package with Scan function and ScanResult type
  - `internal/scanner/scanner_test.go` - Comprehensive test suite (10 test cases)
  - `main.go` - Integrated scanner, displays project count on startup
- **Tests added:**
  - TestScan_FindsPrdJsonFiles - Tests recursive discovery in nested directories
  - TestScan_IgnoresOtherFiles - Verifies only exact "prd.json" matches
  - TestScan_EmptyDirectory - Tests empty directory handling
  - TestScan_NonExistentDirectory - Tests error for invalid paths
  - TestScan_FileInsteadOfDirectory - Tests NotADirectoryError
  - TestScan_ReturnsAbsolutePaths - Verifies paths are absolute
  - TestScan_HandlesUnreadableDirectories - Tests graceful error handling
  - TestScanResult_Count - Tests Count() method
  - TestScanResult_HasErrors - Tests HasErrors() method
  - TestNotADirectoryError - Tests error message format
- **Learnings:**
  - `filepath.WalkDir` is preferred over `filepath.Walk` (more efficient, added in Go 1.16)
  - Use `d.Name()` instead of `filepath.Base(path)` when inside WalkDir callback
  - Return struct with both results and errors for non-fatal error handling
  - Custom error types allow callers to check for specific error conditions
  - Module name `lanham/prdmonitor` must be used in all internal imports
---

## 2026-01-18 - PM-003
- **What was implemented:** JSON parsing and validation for prd.json files
- **Files changed:**
  - `internal/parser/parser.go` - New package with ParseFile, ParseBytes, and ParseFiles functions
  - `internal/parser/parser_test.go` - Comprehensive test suite (18 test cases)
  - `main.go` - Integrated parser to parse all discovered files and display user story counts
- **Tests added:**
  - TestParseBytes_ValidJSON - Tests parsing of complete valid JSON
  - TestParseBytes_ExtractsProjectName - Tests project name extraction
  - TestParseBytes_ExtractsUserStoryFields - Tests all user story fields
  - TestParseBytes_MultipleUserStories - Tests parsing multiple stories
  - TestParseBytes_EmptyUserStories - Tests empty story array
  - TestParseBytes_MalformedJSON - Tests various malformed JSON inputs
  - TestParseBytes_MissingFields - Tests graceful handling of missing fields
  - TestParseFile_ValidFile - Tests file reading and parsing
  - TestParseFile_NonExistentFile - Tests error for missing files
  - TestParseFile_MalformedFile - Tests error for invalid JSON in files
  - TestParseError_Error - Tests error message formatting
  - TestParseError_Unwrap - Tests error unwrapping
  - TestParseFiles_MultipleFiles - Tests batch parsing
  - TestParseFiles_WithErrors - Tests mixed valid/invalid file parsing
  - TestParseFiles_NonExistentFiles - Tests all files failing
  - TestParseFiles_EmptyList - Tests empty input
  - TestParseBytes_AllStatusValues - Tests all status value parsing
- **Learnings:**
  - Use `os.ReadFile()` for simple file reading (replaces ioutil.ReadFile in newer Go)
  - Custom error types should implement `Unwrap()` for error chain inspection
  - Use `errors.As()` to check for specific error types (not type assertion)
  - `encoding/json` gracefully handles missing JSON fields with zero values
  - Batch processing pattern: return `(results, errors)` tuple for non-fatal error handling
  - Test table-driven tests with `t.Run()` for clearer test organization
---

## 2026-01-18 - PM-004
- **What was implemented:** Terminal UI with Kanban board layout using Bubble Tea
- **Files changed:**
  - `internal/tui/app.go` - Main Bubble Tea model with Model-Update-View pattern
  - `internal/tui/board.go` - Kanban board with three columns, distributes cards by status
  - `internal/tui/column.go` - Column component with styling, card rendering, and truncation
  - `internal/tui/tui_test.go` - Comprehensive test suite (13 test cases)
  - `main.go` - Integrated TUI launch after parsing
  - `go.mod`, `go.sum` - Added Bubble Tea and Lip Gloss dependencies
- **Tests added:**
  - TestNewColumn - Tests column creation
  - TestColumn_AddCard - Tests adding cards to columns
  - TestNewBoard_DistributesCardsByStatus - Tests status-based card distribution
  - TestNewBoard_UnknownStatusDefaultsToIncomplete - Tests unknown status handling
  - TestNewBoard_MultipleProjects - Tests aggregation from multiple PRDs
  - TestNewBoard_EmptyParseResults - Tests empty input handling
  - TestColumn_View_EmptyColumn - Tests empty column placeholder message
  - TestColumn_View_WithCards - Tests column rendering with cards
  - TestTruncate - Tests string truncation helper
  - TestNewApp - Tests app initialization
  - TestApp_Init - Tests Bubble Tea init
  - TestBoard_SetSize - Tests responsive sizing
- **Learnings:**
  - Bubble Tea uses `tea.Model` interface with Init(), Update(), View() methods
  - `tea.WithAltScreen()` provides clean terminal enter/exit
  - `tea.WindowSizeMsg` is sent when terminal resizes - use for responsive layout
  - `tea.KeyMsg` for keyboard input, check `msg.String()` for key name
  - Lip Gloss method chaining pattern: `lipgloss.NewStyle().Bold(true).Foreground(...)`
  - Use `lipgloss.Color("39")` for ANSI color codes (strings, not ints)
  - `lipgloss.JoinHorizontal(lipgloss.Top, ...)` for side-by-side layout
  - `lipgloss.RoundedBorder()` for nice box-drawing borders
  - Column width calculation: `(terminalWidth - margins) / numColumns`
  - TUI testing focuses on data structures since View() output is hard to assert
---
